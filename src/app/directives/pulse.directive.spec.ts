import { PulseDirective } from './pulse.directive';

describe('PulseDirective', () => {
  it('should create an instance', () => {
    const directive = new PulseDirective();
    expect(directive).toBeTruthy();
  });
});
