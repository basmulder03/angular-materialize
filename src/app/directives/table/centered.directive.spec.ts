import { CenteredDirective } from './centered.directive';

describe('CenteredDirective', () => {
  it('should create an instance', () => {
    const directive = new CenteredDirective();
    expect(directive).toBeTruthy();
  });
});
