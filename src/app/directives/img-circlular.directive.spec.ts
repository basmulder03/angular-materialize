import { ImgCirclularDirective } from './img-circlular.directive';

describe('ImgCirclularDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgCirclularDirective();
    expect(directive).toBeTruthy();
  });
});
