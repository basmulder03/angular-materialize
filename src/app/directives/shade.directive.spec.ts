import { ShadeDirective } from './shade.directive';

describe('ShadeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadeDirective();
    expect(directive).toBeTruthy();
  });
});
