import { WavesDirective } from './waves.directive';

describe('WavesDirective', () => {
  it('should create an instance', () => {
    const directive = new WavesDirective();
    expect(directive).toBeTruthy();
  });
});
