import { PullDirective } from './pull.directive';

describe('PullDirective', () => {
  it('should create an instance', () => {
    const directive = new PullDirective();
    expect(directive).toBeTruthy();
  });
});
