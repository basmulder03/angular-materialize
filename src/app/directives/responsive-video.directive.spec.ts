import { ResponsiveVideoDirective } from './responsive-video.directive';

describe('ResponsiveVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveVideoDirective();
    expect(directive).toBeTruthy();
  });
});
