import { FlowTextDirective } from './flow-text.directive';

describe('FlowTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FlowTextDirective();
    expect(directive).toBeTruthy();
  });
});
