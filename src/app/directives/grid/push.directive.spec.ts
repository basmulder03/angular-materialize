import { PushDirective } from './push.directive';

describe('PushDirective', () => {
  it('should create an instance', () => {
    const directive = new PushDirective();
    expect(directive).toBeTruthy();
  });
});
