import { ValignDirective } from './valign.directive';

describe('ValignDirective', () => {
  it('should create an instance', () => {
    const directive = new ValignDirective();
    expect(directive).toBeTruthy();
  });
});
