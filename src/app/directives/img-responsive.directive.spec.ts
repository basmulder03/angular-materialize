import { ImgResponsiveDirective } from './img-responsive.directive';

describe('ImgResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
