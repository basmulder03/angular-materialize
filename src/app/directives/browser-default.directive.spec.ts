import { BrowserDefaultDirective } from './browser-default.directive';

describe('BrowserDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new BrowserDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
