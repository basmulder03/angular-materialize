import { StripedDirective } from './striped.directive';

describe('StripedDirective', () => {
  it('should create an instance', () => {
    const directive = new StripedDirective();
    expect(directive).toBeTruthy();
  });
});
